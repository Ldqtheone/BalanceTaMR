{% extends 'base.html.twig' %}

{% block title %}TeamProject{% endblock %}

{% block body %}
    <h1>TeamProject</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Identifiant de projet GitLab</th>
                <td>{{ team_project.id }}</td>
            </tr>
            <tr>
                <th>Nom du projet GitLab</th>
                <td>{{ team_project.projectName }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('team_project_index') }}">back to list</a>

    <a href="{{ path('team_project_edit', {'id': team_project.id}) }}">edit</a>

    {{ include('team_project/_delete_form.html.twig') }}
{% endblock %}
