{% extends 'base.html.twig' %}

{% block title %}TeamProject index{% endblock %}

{% block body %}
    <h1>TeamProject index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Project_id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for team_project in team_projects %}
            <tr>
                <td>{{ team_project.id }}</td>
                <td>{{ team_project.projectId }}</td>
                <td>
                    {#<a href="{{ path('team_project_show', {'id': team_project.id}) }}">show</a>
                    <a href="{{ path('team_project_edit', {'id': team_project.id}) }}">edit</a>#}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    {#<a href="{{ path('team_project_new') }}">Create new</a>#}
{% endblock %}
